<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mr">
    <h3>
        <%= form.label :title %>
    </h3>
    <%= form.text_field :title , class: "form-control", style: "width: 1015px; max-width: unset;" %>
  </div>

  <div class="field mr">
  <h3>
    <%= form.label :description   %>
  </h3>
    <%= form.text_area :description , class: "form-control", rows: "10", cols: "100", style: "max-width: unset;" %>
  </div>

  <div class="field mr">
  <h3>
      <%= form.file_field :post_image %>
  </h3>
  </div>
  <div>
    <%= form.submit class: "btn" %>
  </div>
<% end %>